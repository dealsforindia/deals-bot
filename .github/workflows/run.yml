name: Deal Hunter

on:
  schedule:
    - cron: '*/7 * * * *'  # Checks every 7 minutes
  workflow_dispatch:       # Allows manual button click

permissions:
  contents: write          # Allows saving the memory files

jobs:
  run-bot:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install Libraries
        run: pip install requests feedparser

      - name: Run Deal Bot
        env:
          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
          CHANNEL_ID: ${{ secrets.CHANNEL_ID }}
          EARNKARO_TOKEN: ${{ secrets.EARNKARO_TOKEN }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: python bot.py

      - name: Save Memory (Commit Changes)
        run: |
          git config --global user.name "DealBot"
          git config --global user.email "bot@noreply.github.com"
          git pull origin main --rebase || echo "Already up to date"
          git add last_post_dealsforindia.txt last_post_dealsoffersfreebies.txt last_post_lootdealsforindia.txt seen_deals.json
          git commit -m "Update memory [skip ci]" || exit 0
          git push
